Assistant: ### Code Explanation
This program uses a simple `if-elif-else` chain to handle the user's choices. The program first asks the user for their choice of operation, then performs the corresponding operation on two numbers entered by the user. The result of the operation is then printed to the console. The program also includes error checking to handle cases where the user tries to divide by zero, which is handled by catching the `ValueError` exception and printing an error message.

The `try-except` block is used to catch the `ValueError` exception, which is raised when the user tries to divide by zero. The `except` block catches this exception and prints an error message. The `if` statements at the end of the program handle the user's choices and perform the corresponding operation. The `if __name__ == "__main__":` line ensures that the `main` function is only executed when the script is run directly (i.e., not when it is imported as a module by another script).